import React from "react";
import { Installer } from "./Installer";

export const PackagesPane: React.FC = () => {
    return (
        <div className="Packages">
            <Installer />
        </div>
    );
};
